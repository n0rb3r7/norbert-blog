---
label: AboutMe
hide_body: false
fields:
- name: title
  type: text
  config:
    required: false
  label: title
- name: image
  type: file
  config:
    maxSize: 64
  label: image
- name: blocks
  type: blocks
  label: blocks
  template_types: []
  config:
    min: 
    max: 
- name: hero
  type: include
  config: {}
  template: home-hero
  label: hero
- name: gallery
  type: image_gallery
  config: {}
  label: gallery
pages:
- content/about.md
